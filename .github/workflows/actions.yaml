name: javascript actions

on: [push]

jobs:
  run-github-actions:
    runs-on: ubuntu-latest
    steps:
      - name: simple JS
        uses: actions/hello-world-javascript-action@main
        with:
          who-to-greet: John
